<circle id="@Circle.Id.ToString()" cx="@Circle.Cx" cy="@Circle.Cy" r="@Circle.R" stroke="black" stroke-width="3" fill="@Circle.Fill" />

@code {
    [Parameter]
    public Circle Circle { get; set; }

    protected override Task OnInitializedAsync()
    {
        return base.OnInitializedAsync();
    }
}